NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Gates the whole app behind a single username/password (see middleware.ts).
# Pick your own values — set these in Vercel's project settings for the
# real deployment, not in this file.
BASIC_AUTH_USER=your-chosen-username
BASIC_AUTH_PASSWORD=your-chosen-password
